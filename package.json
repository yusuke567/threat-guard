{
  "name": "threat-guard",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev:api": "npm run dev -w @threat-guard/api",
    "build": "npm run build -w @threat-guard/shared && npm run build -w @threat-guard/api"
  }
}
