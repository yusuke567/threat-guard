# PostgreSQL (default — used in production and Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/brand_shield?schema=public"

# SQLite (for quick local dev without PostgreSQL)
# DATABASE_URL="file:./prisma/dev.db"

PORT=3001

SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@brandshield.app"

ANTHROPIC_API_KEY=""

JWT_SECRET=change-me-in-production
