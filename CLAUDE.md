# CLAUDE.md

## 投稿禁止ルール

1. **途中経過メッセージの抑制** — 「〇〇します」「〇〇中」の連投禁止。完了後に1メッセージで「やったこと・結果・次」を報告。5分以上かかる場合のみ途中報告OK

2. **セッション開始時チェック** — 作業開始前にこのファイルの「投稿禁止ルール」を1回読み直す

## 開発ルール

3. **類似機能追加前の既存機能確認** — 新機能を追加する前に、既存コードに同様の機能がないか確認する。重複実装による手戻りを防ぐ

4. **認証系UI変更時のモバイル確認必須** — ログイン・サインアップ等の認証UIを変更した場合、デスクトップだけでなくモバイルブラウザでの動作確認を必ず行う

5. **PRマージ後のmain反映確認必須** — PRをマージした後、`git log main`で反映を確認し、本番/プレビューURLで実際の動作を確認する

## デプロイ・確認ルール

6. **Vercelプレビュー確認依頼時は必ずプレビューURLを直接貼る** — `threat-guard-<hash>-yusuke567s-projects.vercel.app` 形式。プロジェクト名は `threat-guard` のみ
