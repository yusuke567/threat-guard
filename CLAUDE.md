# CLAUDE.md

## 開発ルール

### Slack投稿ルール
- **途中経過メッセージの投稿禁止** — 「〇〇します」「〇〇中」「〇〇確認:」の連投禁止。完了後に1メッセージで「やったこと・結果・次」を報告。5分以上かかる場合のみ途中報告OK
- **セッション開始時チェック** — 作業開始前にこのCLAUDE.mdのルールを1回読み直す

### Vercel
- **プレビュー確認依頼時は必ずプレビューURLを直接貼る** — `threat-guard-<hash>-yusuke567s-projects.vercel.app` 形式。プロジェクト名は `threat-guard` のみ

### 機能追加・変更時
- **類似機能追加前の既存機能確認** — 新機能を実装する前に、既存コードに同様の機能がないか確認する。重複実装によるリファクタ手戻りを防ぐ
- **認証系UI変更時のモバイル確認必須** — ログイン・認証関連のUI変更後は、モバイルブラウザ（iPhone Safari等）での動作確認を必ず行う

### Git・PR・デプロイ
- **PRマージ後のmain反映確認必須** — PRがmainにマージされたら `git log main` で反映を確認し、本番URLでも動作確認する。PRを作成・承認しただけで完了としない
