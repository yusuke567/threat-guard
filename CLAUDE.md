# CLAUDE.md - ThreatGuard プロジェクトルール

## Git/GitHub操作ルール

### PRマージ確認ルール（必須）

PRをマージしたと報告する前に、必ず以下を確認すること：

1. **マージ操作の実行確認**: `gh pr merge`またはGitHub UIでマージを実行したか？
2. **PR状態の確認**: `gh pr view <PR番号>`で`merged: true`になっているか？
3. **mainブランチの確認**: `git fetch origin main && git log origin/main -1`でHEADがマージコミットになっているか？

**禁止事項**:
- PRを作成しただけで「マージ済み」と報告しない
- mainが「リモートと同期している」だけで「マージ済み」と報告しない

**報告の区別**:
- 「PRを作成しました」 → PR作成時
- 「マージを実行しました」 → マージ操作実行時
- 「mainにマージコミットが反映されました」 → 検証完了時

### デプロイ確認ルール

- Vercelプレビュー確認依頼時は必ずプレビューURLを直接貼る
- プロジェクト名は `threat-guard` のみ

## コミュニケーションルール

### 途中経過メッセージの抑制

- 「〇〇します」「〇〇中」「まず〜を確認します」の連投禁止
- 完了後に1メッセージで「やったこと・結果・次」を報告
- 5分以上かかる場合のみ途中報告OK

### セッション開始時

- 作業開始前にこのCLAUDE.mdを読み直す
